# Regression Log

- state=active | target=FS-230-HDS-010-SDS-010-SMS-040 policy-neutral isolated service endpoint | live_evidence=retry-6 cold-staged exact pushed pins on all three isolated hosts; the IPv6 UDP/4242 datagram crossed all five NixOS routers and reached the selected service endpoint, while the listener answered the same address locally, but the endpoint firewall sent the remote datagram to `nixos-fw-log-refuse`; TCP/4242, other UDP, reverse-new and outbound negatives all remained denied | first_bad_boundary=`mkBaseEndpoint` leaves the NixOS default firewall active even though the renderer contract forbids endpoint firewall-policy invention | required_fix=make endpoint containers policy-neutral without deriving a tuple-specific allow, add a focused seeded negative, run the full renderer suite, push, cold-stage, and repeat the NixOS/CLAB live matrix without a hotpatch.
- state=solved | target=FS-970-HDS-010-SDS-020-SMS-040 deterministic DHCPv6 reservation client identity | construction_evidence=`NETWORK_REPO_DIRECT_TEST_OK=1 ./run-all-tests.sh` passes 15/15, including the focused row that proves the DHCPv6 endpoint emits a stable link-layer DUID, stateful solicitation, and `IPv6PrivacyExtensions=false` | prior_live_failure=the isolated VLAN398 cold-stage run through CLAB `3aa56a0759ea8ace38b6285288b97edfac9558d7` acquired the enrolled DHCPv6 address but also emitted one temporary global address because privacy extensions remained implicit | live_evidence=after pushing `697cb43c8d17315adc2b7b8d2173f53b8089fc9c`, all three isolated hosts were observed down and returned with changed boot IDs and exact staged source hashes; `FS-970-HDS-010-SDS-020` then passed on both real NixOS and CLAB substrates with stable protected MAC/DUID/IAID/IID identity, one predictable IPv4 address, one predictable IPv6 address, and zero additional global addresses.
